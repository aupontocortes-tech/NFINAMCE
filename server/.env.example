# Configuração do Servidor
PORT=3001
NODE_ENV=development

# Segurança - JWT Secret (MUDE EM PRODUÇÃO!)
JWT_SECRET=nfinance-secret-key-change-me-in-production

# Banco de Dados
# Em desenvolvimento usa SQLite (automático)
# Em produção, configure DATABASE_URL com PostgreSQL
# DATABASE_URL=postgresql://user:password@host:port/database

# Resend API (Recomendado para envio de emails)
# Obtenha sua chave em: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx

# SMTP (Fallback se não usar Resend)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu-email@gmail.com
# SMTP_PASS=sua-senha-app
